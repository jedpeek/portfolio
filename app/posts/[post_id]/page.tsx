"use client";
// import { useParams } from "next/navigation";
// import { BLOG_DATA } from "/BLOG_DATA";

const Post = () => {
  // const data = BLOG_DATA;
  // const { post_id } = useParams();
  // const post = data.find((post: any) => post.post_id === post_id);
  return (
    <>
      {/* {post ? (
        <div>
          <h1>AUTHOR: {post.author}</h1>
          <div>
            <h1>TITLE: {post.title}</h1>
          </div>
          <div>
            <p>{post.content}</p>
          </div>
        </div>
      ) : (
        <h1>CAN'T FIND POST</h1>
      )} */}
    </>
  );
};

export default Post;
